<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= actor.first_name %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <%- include("./partials/header") %>  
    <h1><%= actor.first_name %> </h1>
    <br>
    <p>NAME: <%= actor.first_name %> </p>
    <br>
    <p>LASTNAME: <%= actor.last_name %> </p>
    <br>
    <p>RATING: <%= actor.rating %> </p>
    <br>
    <% if (actor.peliculas.length > 0) { %>
        <p>PELICULAS:</p><br>
       <% actor.peliculas.forEach(pelicula => { %>
           <ul>
               <a href="/movies/detail/<%= pelicula.id %>"><li><%= pelicula.title %></li></a>
           </ul>
        <% }) %>
    <% } else{ %>
        <p>Este actor no participo en ninguna pelicula</p>
    <% } %> 
</body>
</html>