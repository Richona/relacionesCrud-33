<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= genre.name %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <%- include("./partials/header") %>    
    <h1><%= genre.name %> </h1>
    <br>
    <p>NAME: <%= genre.name %> </p>
    <br>
    <p>RANKIG: <%= genre.ranking %> </p>
    <br>
    <% if (genre.peliculas.length > 0) { %>
     <p>PELICULAS:</p><br>
    <% genre.peliculas.forEach(pelicula => { %>
        <ul>
            <a href="/movies/detail/<%= pelicula.id %>"><li><%= pelicula.title %></li></a>
        </ul>
    <% }) %>
    <% } else{ %>
        <p>Este genero no tiene peliculas</p>
    <% } %> 
    <br>
    <section>
        <!--Aquí debe colocar los botones de Modificar - Borrar y Listado de Películas-->
        <p> 
            <button class="botonAgregar" type="submit"><a href="/genres/edit/<%= genre.id %>">Editar</a></button>
            <button class="botonBorrar" type="submit"><a href="/genres/delete/<%= genre.id %>">Borrar</a></button>
        </p>
    </section>
</body>
</html>